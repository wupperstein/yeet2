\documentclass{article}

\usepackage{pgfplotstable}

\begin{document}

\pgfplotstabletypeset[
    columns/0/.style={column name={$\theta/ ~^\circ$}},
    columns/1/.style={column name={$\Delta\theta/ ~^\circ$}},
    header=false,
    string type,
    before row=\hline,
    every last row/.style={after row=\hline},
    column type/.add={|}{},
    every last column/.style={column type/.add={}{|}}
]{data.txt}


\end{document}